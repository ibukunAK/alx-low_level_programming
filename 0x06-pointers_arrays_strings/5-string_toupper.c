#include "main.h"
#include <stdio.h>

/**
 * string_toupper - functions to change all lowercase to uppercase
 * @n: pointer
 * Return: n
 */

char *string_toupper(char *)
{       
int i;  

i = 0;
while (n[i] >= 'a' && n[i] <= 'z')
n[i] = n[i] - 32;
i++;
}
return (n);
}
